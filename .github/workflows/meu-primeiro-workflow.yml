name: Primeiro Workflow Linuxtips

on:
  push:
    branches:
      - main

jobs: 
  giropops:
    runs-om: ubunto-latest
    steps:
      - name: Confere version do ubunto
        run: cat /etc/os-release

      - name: Exibir uma mensagem
        run: echo "Estou fazendo o curso do Github Actions de Linuxtips"